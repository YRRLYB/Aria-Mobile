# 更新记录

## 0.3.0-beta.0(体验大版本)

- 修复切歌竞态:切歌瞬间先暂停旧流再解析新流,连续点歌不再"还是那一首";无缝预加载的哨兵地址也会先解析成真实链接。
- 登录方式精简为验证码 + 二维码(移除密码登录);扫码登录凭据捕获已修复。
- 状态栏适配重做:安全区尺寸在页面加载后多次重推 + JS 可主动拉取,标题/头像不再与时钟重叠。
- 播放页:歌名上方显示当前歌词一行(隐蔽),点按封面全屏歌词流畅放大、背景模糊;爱心显示真实喜欢状态;队列面板带封面与绿色循环标识;随机/循环激活为绿色。
- 新增「手机音乐」:MediaStore 扫描本机音频,离线直接播放。
- 首页/搜索排版优化,搜索新增歌手卡片;设置页隐藏直连模式下的桌面连接项,显示版本号。
- 常驻播放:首次播放时请求电池优化豁免,减少后台被杀。
- 返回手势改为应用内返回(播放页 → 首页),不再直接退出。

## 0.2.2-beta.0(验证码登录)

- 直连登录新增**短信验证码**方式(默认),与密码登录、二维码登录并列;发送按钮带 60 秒倒计时。

## 0.2.1-beta.0(登录修复与密码登录)

- 修复直连扫码登录后仍是「未登录」的问题:登录凭据在响应 Set-Cookie 头里,此前没有捕获,导致 cookie 存了空串。
- 新增手机号 + 密码直连登录(手机上扫码体验差,现在两种方式都可用;密码仅在本机 MD5 后参与登录,不落盘)。

## 0.2.0-beta.0(手机直连网易云)

- 网易云直连:手机原生接入网易云(扫码登录/喜欢/每日推荐/私人漫游/歌单/搜索/歌词/取流),不再强依赖电脑;桌面伴侣模式保留,设置里可切换。
- 真沉浸式刘海适配:页面渐变直接画到状态栏后面,安全区由原生注入,状态栏图标随页面明暗自动切换。
- 桌面同款自适应图标与品牌启动屏;播放页显示真实码率/采样率,音质档位与 HiFi 开关真正作用于取流。
- 加密层与 Node 原版逐字节一致(7 项 golden 单测钉死),模块自带 NETEASE_API.md 维护指南。

# 更新记录

## 0.4.0-beta.0(移动版起步)

- 新增 Aria Mobile(Android 伴侣 App,`mobile/` 目录):局域网连接桌面端,同步网易云喜欢/每日推荐/私人漫游/歌单,串流播放电脑上的本地曲库。
- 移动端 0.1.4:刘海/状态栏适配(WebView 强制预留系统栏空间,不再与时钟电量重叠);桌面同款自适应图标与启动屏;播放页显示真实码率与采样率,音质档位/HiFi 开关真正作用于取流(下一曲生效)。
- 制定移动端企划(docs/mobile-plan.md):双端独立(Kotlin 原生网易云接入)+ Cloudflare 免费中继同步,里程碑 M1-M4。
- 移动端 0.1.1:修复快速点按切歌触发的崩溃(前台服务未在系统宽限期内调用 startForeground 被系统杀死;现在 LOAD 意图到达立即进入前台,播放就绪后交还媒体通知)。
- 移动端 0.1.1:播放页重做为网易云式沉浸设计——封面全屏铺满、按封面主色渐变入黑,配套暗色控制区、歌词页与队列面板;快速切歌增加防抖保护。
- 移动端 0.1.2/0.1.3:修复真机上封面显示为破碎图标的问题(WebView 页面源从 https 改为 http,消除对局域网 http 图片的混合内容拦截;图片加载失败时回退为字母占位而非破碎图标);修复设置页不显示网易云账号(启动时未拉取账号信息);点锁屏媒体卡片可打开 App;启动自动恢复上次播放曲目。
- 桌面端设置新增「手机远程访问」卡片:开关、服务器地址、连接令牌与配对二维码;开启后后端绑定 0.0.0.0 并重启。
- 后端新增远程访问鉴权:非回环请求需携带令牌(`Authorization: Bearer` 或 `?token=`,媒体标签适用),本机桌面端访问不受影响;`GET /api/remote-access` 返回配置与局域网地址。
- 前端连接层支持运行时配置服务器地址与令牌(localStorage `aria-connection`),桌面版行为不变。
- 移动端音频由原生前台服务承载(Media3/ExoPlayer + MediaSession):后台播放、通知栏/锁屏控制、无缝预加载下一首;浏览器内回退为 HTML5 audio。

## 0.3.13-beta.0

- 修复 Windows 音量面板的音乐卡片：浏览器、WASAPI 共享和独占播放统一使用 Aria 的媒体会话，正确显示歌曲、歌手、专辑和封面，并支持系统播放、暂停及切歌。
- 关闭 mpv 自带的重复媒体卡片；重复的系统播放/暂停命令不再反向切换播放状态，切换到无封面的歌曲会清除旧封面。
- 快速播放/暂停时以最新操作为准，旧的原生播放器回执不会覆盖新操作。
- 修复缓存队列恢复后、曲库尚未加载时切歌丢失当前歌曲的问题。
- 修复同一首歌切换输出模式时可能继续使用旧 WASAPI 模式的问题，避免旧进程退出事件干扰新播放器。
- 播放进度与动态码率更新不再反复刷新整个界面；进度条、歌词和频谱继续按需更新。
- 本地封面按列表和播放器尺寸生成磁盘缩略图，保留原始封面；限制图片解码并发，合并重复封面请求，按需加载图片处理模块。
- 切回浏览器播放或清空播放内容时释放闲置 mpv 进程。

验证方法和内存测试范围见 [验证记录](docs/verification-0.3.13-beta.0.md)。
